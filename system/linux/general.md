* inotify(inode notify): a Linux kernel subsystem that acts to extend filesystems to notice changes to the filesystem, and report those changes to applications.
	* [wiki](https://en.wikipedia.org/wiki/Inotify)